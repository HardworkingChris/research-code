# This file is simply a save space for the commands involved in plotting
# a 3D scatter plot when you have 3 same-length vectors

import matplotlib.pyplot as plt
from mpl_toolkits.mplot3d import Axes3D
fig = plt.figure()
ax = fig.add_subplot(111, projection='3d')
ax.scatter(x,y,z)

# In an interactive pylab environment, or when using Brian, the following
# also works (simpler)

from mpl_toolkits import mplot3d
ax = subplot(111, projection='3d')
ax.scatter3D(x,y,z)
